---
title: "M3 Problem Set"
output: pdf_document
date: "2024-02-05"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


# Loading
```{r}
data(trees)
```

# Print the first 6 rows
```{r}
head(trees)
```

1. Using the nrow() command, how many rows does the data.frame have?

2. Using the nrow() command, how many columns are there in the trees dataset?

3. What information does the str(trees) command provide about the structure of the trees data frame in R?
